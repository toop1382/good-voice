version: '3.8'

services:
  voice-server:
    build: .
    restart: unless-stopped
    ports:
      - "50005:50005/udp"
      - "50006:50006"
      - "50007:50007"
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
